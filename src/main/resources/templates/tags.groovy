package templates

static def yield(text) {}

static def yieldUnescaped(text) {}

static def html(parms = [:], supplier = [:]) {}

static def head(parms = [:], supplier = [:]) {}

static def meta(parms = [:], supplier = [:]) {}

static def title(parms = [:], supplier = [:]) {}

static def a(parms = [:], supplier = [:]) {}

static def abbr(parms = [:], supplier = [:]) {}

static def address(parms = [:], supplier = [:]) {}

static def area(parms = [:], supplier = [:]) {}

static def audio(parms = [:], supplier = [:]) {}

static def bm(parms = [:], supplier = [:]) {}

static def cite(parms = [:], supplier = [:]) {}

static def code(parms = [:], supplier = [:]) {}

static def del(parms = [:], supplier = [:]) {}

static def details(parms = [:], supplier = [:]) {}

static def dfn(parms = [:], supplier = [:]) {}

static def command(parms = [:], supplier = [:]) {}

static def em(parms = [:], supplier = [:]) {}

static def font(parms = [:], supplier = [:]) {}

static def i(parms = [:], supplier = [:]) {}

static def iframe(parms = [:], supplier = [:]) {}

static def img(parms = [:], supplier = [:]) {}

static def input(parms = [:], supplier = [:]) {}

static def ins(parms = [:], supplier = [:]) {}

static def keygen(parms = [:], supplier = [:]) {}

static def kdb(parms = [:], supplier = [:]) {}

static def label(parms = [:], supplier = [:]) {}

static def legend(parms = [:], supplier = [:]) {}

static def link(parms = [:], supplier = [:]) {}

static def mark(parms = [:], supplier = [:]) {}

static def meter(parms = [:], supplier = [:]) {}

static def nav(parms = [:], supplier = [:]) {}

static def optgroup(parms = [:], supplier = [:]) {}

static def q(parms = [:], supplier = [:]) {}

static def small(parms = [:], supplier = [:]) {}

static def select(parms = [:], supplier = [:]) {}

static def source(parms = [:], supplier = [:]) {}

static def span(parms = [:], supplier = [:]) {}

static def strong(parms = [:], supplier = [:]) {}

static def sub(parms = [:], supplier = [:]) {}

static def summary(parms = [:], supplier = [:]) {}

static def sup(parms = [:], supplier = [:]) {}

static def td(parms = [:], supplier = [:]) {}

static def time(parms = [:], supplier = [:]) {}

static def var(parms = [:], supplier = [:]) {}

static def article(parms = [:], supplier = [:]) {}

static def aside(parms = [:], supplier = [:]) {}

static def blockquote(parms = [:], supplier = [:]) {}

static def body(parms = [:], supplier = [:]) {}

static def br(parms = [:], supplier = [:]) {}

static def button(parms = [:], supplier = [:]) {}

static def canvas(parms = [:], supplier = [:]) {}

static def caption(parms = [:], supplier = [:]) {}

static def col(parms = [:], supplier = [:]) {}

static def colgroup(parms = [:], supplier = [:]) {}

static def dd(parms = [:], supplier = [:]) {}

static def div(parms = [:], supplier = [:]) {}

static def dl(parms = [:], supplier = [:]) {}

static def dt(parms = [:], supplier = [:]) {}

static def embed(parms = [:], supplier = [:]) {}

static def fieldset(parms = [:], supplier = [:]) {}

static def figcaption(parms = [:], supplier = [:]) {}

static def figure(parms = [:], supplier = [:]) {}

static def footer(parms = [:], supplier = [:]) {}

static def form(parms = [:], supplier = [:]) {}

static def h1(parms = [:], supplier = [:]) {}

static def h2(parms = [:], supplier = [:]) {}

static def h3(parms = [:], supplier = [:]) {}

static def h4(parms = [:], supplier = [:]) {}

static def h5(parms = [:], supplier = [:]) {}

static def h6(parms = [:], supplier = [:]) {}

static def header(parms = [:], supplier = [:]) {}

static def hgroup(parms = [:], supplier = [:]) {}

static def hr(parms = [:], supplier = [:]) {}

static def li(parms = [:], supplier = [:]) {}

static def map(parms = [:], supplier = [:]) {}

static def object(parms = [:], supplier = [:]) {}

static def ol(parms = [:], supplier = [:]) {}

static def output(parms = [:], supplier = [:]) {}

static def p(parms = [:], supplier = [:]) {}

static def pre(parms = [:], supplier = [:]) {}

static def progress(parms = [:], supplier = [:]) {}

static def section(parms = [:], supplier = [:]) {}

static def table(parms = [:], supplier = [:]) {}

static def tbody(parms = [:], supplier = [:]) {}

static def textarea(parms = [:], supplier = [:]) {}

static def tfoot(parms = [:], supplier = [:]) {}

static def th(parms = [:], supplier = [:]) {}

static def thead(parms = [:], supplier = [:]) {}

static def tr(parms = [:], supplier = [:]) {}

static def ul(parms = [:], supplier = [:]) {}

static def video(parms = [:], supplier = [:]) {}

static def text(parms = [:], supplier = [:]) {}
